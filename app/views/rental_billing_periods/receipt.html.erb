<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NOTA DE DÉBITO - <%= @rental.generate_debit_note_number(@billing_period) %></title>
  <style>
    @media print {
      body { margin: 0; }
      .no-print { display: none; }
    }
    
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 10px;
      background: white;
      color: #333;
      line-height: 1.2;
      font-size: 10px;
    }
    
    .container {
      max-width: 100%;
      margin: 0 auto;
      border: 1px solid #333;
      padding: 10px;
      min-height: 100vh;
      box-sizing: border-box;
    }
    
    .header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 15px;
      border-bottom: 1px solid #333;
      padding-bottom: 10px;
      page-break-inside: avoid;
    }
    
    .logo-section {
      text-align: center;
    }
    
    .logo-placeholder {
      width: 80px;
      height: 40px;
      border: 1px solid #ccc;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 5px;
      font-size: 8px;
      color: #666;
    }
    
    .company-name {
      font-size: 14px;
      font-weight: bold;
      color: #333;
    }
    
    .company-info {
      text-align: right;
      font-size: 8px;
    }
    
    .company-info p {
      margin: 1px 0;
    }
    
    .document-title {
      text-align: center;
      font-size: 18px;
      font-weight: bold;
      margin: 15px 0;
      text-transform: uppercase;
      page-break-inside: avoid;
    }
    
    .section {
      margin-bottom: 12px;
      page-break-inside: avoid;
    }
    
    .section-title {
      font-weight: bold;
      font-size: 11px;
      margin-bottom: 5px;
      text-transform: uppercase;
      background: #f0f0f0;
      padding: 2px 5px;
    }
    
    .info-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
    }
    
    .info-item {
      display: flex;
      justify-content: space-between;
      border-bottom: 1px solid #eee;
      padding: 2px 0;
      font-size: 9px;
    }
    
    .info-label {
      font-weight: bold;
      font-size: 9px;
    }
    
    .info-value {
      font-size: 9px;
    }
    
    .bank-info {
      background: #f9f9f9;
      padding: 8px;
      border-radius: 3px;
      margin: 10px 0;
    }
    
    .billing-description {
      background: #f9f9f9;
      padding: 8px;
      border-radius: 3px;
      margin: 10px 0;
    }
    
    .description-title {
      font-weight: bold;
      margin-bottom: 5px;
    }
    
    .description-content {
      font-size: 9px;
      line-height: 1.3;
    }
    
    .description-content p {
      margin: 2px 0;
    }
    
    .amount {
      font-weight: bold;
      color: #059669;
    }
    
    .actions {
      text-align: center;
      margin-top: 20px;
      padding-top: 15px;
      border-top: 1px solid #eee;
    }
    
    .btn {
      display: inline-block;
      padding: 8px 16px;
      margin: 0 8px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
      font-size: 12px;
    }
    
    .btn-primary {
      background: #3b82f6;
      color: white;
    }
    
    .btn-secondary {
      background: #6b7280;
      color: white;
    }
    
    /* Otimizações específicas para PDF */
    @page {
      margin: 8mm;
      size: A4;
    }
    
    /* Evita quebras de página em seções importantes */
    .header, .document-title, .section {
      page-break-inside: avoid;
    }
    
    /* Força o conteúdo a caber em uma página */
    .container {
      height: 100vh;
      overflow: hidden;
    }
    
    /* Compacta ainda mais seções específicas */
    .section.compact {
      margin-bottom: 8px;
    }
    
    .section.compact .info-grid {
      gap: 5px;
    }
    
    .section.compact .info-item {
      padding: 1px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Cabeçalho -->
    <div class="header">
      <div class="logo-section">
        <div class="logo-placeholder">[Logo]</div>
        <div class="company-name">RADIAL ENERGIA</div>
      </div>
      
      <div class="company-info">
        <p><strong>FONTES ENERGIA COMÉRCIO E MANUTENÇÃO LTDA - ME</strong></p>
        <p>Estrada São Miguel Arcanjo, 140 - Veraneio Maracanã</p>
        <p>Itaquaquecetuba - SP CEP: 08582-500</p>
        <p>Fone: (11) 2941-4775 / 9.9837-2639</p>
        <p>www.radialenergia.com.br</p>
        <p>thomas@radialenergia.com.br</p>
      </div>
    </div>
    
    <!-- Título do Documento -->
    <div class="document-title">NOTA DE DÉBITO</div>
    
    <!-- Sacador -->
    <div class="section compact">
      <div class="section-title">SACADOR</div>
      <div class="info-grid">
        <div class="info-item">
          <span class="info-label">NOME RAZÃO SOCIAL:</span>
          <span class="info-value">FONTES ENERGIA COMÉRCIO E MANUTENÇÃO LTDA - ME</span>
        </div>
        <div class="info-item">
          <span class="info-label">CNPJ/CPF:</span>
          <span class="info-value">13.318.529/0001-08</span>
        </div>
        <div class="info-item">
          <span class="info-label">INSCRIÇÃO ESTADUAL:</span>
          <span class="info-value">379.076.526.115</span>
        </div>
        <div class="info-item">
          <span class="info-label">INSCRIÇÃO MUNICIPAL:</span>
          <span class="info-value">35.2.2520868-6</span>
        </div>
      </div>
    </div>
    
    <!-- Sacado -->
    <div class="section compact">
      <div class="section-title">SACADO</div>
      <div class="info-grid">
        <div class="info-item">
          <span class="info-label">NOME RAZÃO SOCIAL:</span>
          <span class="info-value"><%= @rental.client.name %></span>
        </div>
        <div class="info-item">
          <span class="info-label">ENDEREÇO:</span>
          <span class="info-value"><%= @rental.client.address %></span>
        </div>
        <div class="info-item">
          <span class="info-label">MUNICÍPIO:</span>
          <span class="info-value"><%= @rental.client.city %></span>
        </div>
        <div class="info-item">
          <span class="info-label">FONE/FAX:</span>
          <span class="info-value"><%= @rental.client.phone %></span>
        </div>
        <div class="info-item">
          <span class="info-label">UF:</span>
          <span class="info-value"><%= @rental.client.state %></span>
        </div>
        <div class="info-item">
          <span class="info-label">BAIRRO:</span>
          <span class="info-value"><%= @rental.client.neighborhood %></span>
        </div>
        <div class="info-item">
          <span class="info-label">CNPJ/CPF:</span>
          <span class="info-value"><%= @rental.client.document_number %> (<%= @rental.client.document_type %>)</span>
        </div>
        <div class="info-item">
          <span class="info-label">INCR. ESTADUAL:</span>
          <span class="info-value"><%= @rental.client.state_registration || '-' %></span>
        </div>
        <div class="info-item">
          <span class="info-label">CEP:</span>
          <span class="info-value"><%= @rental.client.zip_code %></span>
        </div>
      </div>
    </div>
    
    <!-- Dados da Nota de Débito -->
    <div class="section compact">
      <div class="section-title">DADOS NOTA DE DÉBITO</div>
      <div class="info-grid">
        <div class="info-item">
          <span class="info-label">NOTA DE DÉBITO Nº:</span>
          <span class="info-value"><%= @rental.generate_debit_note_number(@billing_period) %></span>
        </div>
        <div class="info-item">
          <span class="info-label">DATA DE EMISSÃO:</span>
          <span class="info-value"><%= @billing_period.start_date.strftime('%d/%m/%Y') %></span>
        </div>
        <div class="info-item">
          <span class="info-label">VALOR:</span>
          <span class="info-value amount"><%= @billing_period.formatted_amount %></span>
        </div>
        <div class="info-item">
          <span class="info-label">VENCIMENTO:</span>
          <span class="info-value"><%= @billing_period.end_date.strftime('%d/%m/%Y') %></span>
        </div>
        <div class="info-item" style="grid-column: 1 / -1;">
          <span class="info-label">VALOR POR EXTENSO:</span>
          <span class="info-value"><%= valor_por_extenso(@billing_period.amount) %></span>
        </div>
      </div>
    </div>
    
    <!-- Dados Bancários -->
    <div class="section compact">
      <div class="section-title">DADOS BANCÁRIOS PARA PAGAMENTO</div>
      <div class="bank-info">
        <div class="info-grid">
          <div class="info-item">
            <span class="info-label">BANCO:</span>
            <span class="info-value">ITAÚ S/A</span>
          </div>
          <div class="info-item">
            <span class="info-label">AGÊNCIA:</span>
            <span class="info-value">0709</span>
          </div>
          <div class="info-item">
            <span class="info-label">CONTA CORRENTE:</span>
            <span class="info-value">06.339-0</span>
          </div>
          <div class="info-item">
            <span class="info-label">ENDEREÇO:</span>
            <span class="info-value">R. Serra de Bragança, 1856 - Vl. Gomes Cardim - SP</span>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Discriminação da Cobrança -->
    <div class="section compact">
      <div class="section-title">DISCRIMINAÇÃO DA COBRANÇA</div>
      <div class="billing-description">
        <div class="description-content">
          <p><strong>Refere-se a locação de:</strong></p>
          <% @rental.equipments.each do |equipment| %>
            <p>• <%= equipment.display_name %> (Série: <%= equipment.serial_number || 'N/A' %>)</p>
          <% end %>
          <p><strong>Pedido Cliente:</strong> <%= @billing_period.has_client_order? ? @billing_period.client_order : '-' %></p>
          <p><strong>Conforme contrato N°:</strong> <%= @rental.name %></p>
          <p><strong>Nota de remessa N°:</strong> <%= @rental.remessa_note_display %></p>
          <% if @billing_period.has_observations? %>
            <p><strong>Obs Contrato:</strong> <%= @billing_period.observations %></p>
          <% end %>
          <p><strong>Período:</strong> <%= @billing_period.name %></p>
        </div>
      </div>
    </div>
    
    <!-- Ações (apenas para visualização HTML) -->
    <div class="actions no-print">
      <%= link_to "Voltar para Períodos", rental_rental_billing_periods_path(@rental), class: "btn btn-secondary" %>
      <%= link_to "Baixar PDF", receipt_pdf_rental_rental_billing_period_path(@rental, @billing_period, format: :pdf), class: "btn btn-primary" %>
    </div>
  </div>
</body>
</html> 