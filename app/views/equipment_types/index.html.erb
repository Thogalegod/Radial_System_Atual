<div class="equipment-types-page">
  <div class="page-header">
    <div class="header-content">
      <h1>Tipos de Equipamento</h1>
      <p>Gerencie os tipos de equipamento e suas características</p>
    </div>
    <div class="header-actions">
      <%= link_to new_equipment_type_path, class: "btn btn-primary" do %>
        <i class="fas fa-plus"></i>
        Novo Tipo
      <% end %>
    </div>
  </div>

  <div class="equipment-types-grid">
    <% if @equipment_types.any? %>
      <% @equipment_types.each do |equipment_type| %>
        <div class="equipment-type-card" style="<%= equipment_type.css_style %>">
          <div class="card-header">
            <div class="type-icon">
              <i class="<%= equipment_type.icon_class %>"></i>
            </div>
            <div class="type-info">
              <h3><%= equipment_type.name %></h3>
              <p><%= equipment_type.description %></p>
            </div>
            <div class="type-status">
              <% if equipment_type.active? %>
                <span class="status-badge active">Ativo</span>
              <% else %>
                <span class="status-badge inactive">Inativo</span>
              <% end %>
            </div>
          </div>

          <div class="card-stats">
            <div class="stat">
              <span class="stat-number"><%= equipment_type.equipment_count %></span>
              <span class="stat-label">Equipamentos</span>
            </div>
            <div class="stat">
              <span class="stat-number"><%= equipment_type.feature_count %></span>
              <span class="stat-label">Características</span>
            </div>
          </div>

          <div class="card-actions">
            <%= link_to equipment_type_path(equipment_type), class: "btn btn-sm btn-outline" do %>
              <i class="fas fa-eye"></i>
              Ver Detalhes
            <% end %>
            
            <%= link_to equipment_type_equipment_features_path(equipment_type), class: "btn btn-sm btn-outline" do %>
              <i class="fas fa-cog"></i>
              Características
            <% end %>
            
            <%= link_to edit_equipment_type_path(equipment_type), class: "btn btn-sm btn-outline" do %>
              <i class="fas fa-edit"></i>
              Editar
            <% end %>
            
            <%= link_to equipment_type_path(equipment_type), 
                method: :delete, 
                data: { confirm: "Tem certeza que deseja excluir este tipo de equipamento?" },
                class: "btn btn-sm btn-danger" do %>
              <i class="fas fa-trash"></i>
              Excluir
            <% end %>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="empty-state">
        <div class="empty-icon">🏷️</div>
        <h3>Nenhum tipo de equipamento cadastrado</h3>
        <p>Comece criando o primeiro tipo de equipamento para o seu sistema.</p>
        <%= link_to new_equipment_type_path, class: "btn btn-primary" do %>
          <i class="fas fa-plus"></i>
          Criar Primeiro Tipo
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<style>
.equipment-types-page {
  padding: 2rem;
  max-width: 1400px;
  margin: 0 auto;
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 2rem;
  gap: 2rem;
}

.header-content h1 {
  font-size: 2.5rem;
  color: #1f2937;
  margin: 0 0 0.5rem 0;
}

.header-content p {
  color: #6b7280;
  font-size: 1.1rem;
  margin: 0;
}

.header-actions {
  flex-shrink: 0;
}

.equipment-types-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
  gap: 1.5rem;
}

.equipment-type-card {
  background: white;
  border-radius: 12px;
  padding: 1.5rem;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  transition: all 0.2s;
  border-left: 4px solid var(--primary-color, #3b82f6);
}

.equipment-type-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 25px -3px rgba(0, 0, 0, 0.1);
}

.card-header {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.type-icon {
  width: 50px;
  height: 50px;
  background: var(--primary-color, #3b82f6);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.5rem;
  flex-shrink: 0;
}

.type-info {
  flex: 1;
}

.type-info h3 {
  margin: 0 0 0.5rem 0;
  color: #1f2937;
  font-size: 1.25rem;
  font-weight: 600;
}

.type-info p {
  margin: 0;
  color: #6b7280;
  font-size: 0.9rem;
  line-height: 1.4;
}

.type-status {
  flex-shrink: 0;
}

.status-badge {
  padding: 0.25rem 0.75rem;
  border-radius: 20px;
  font-size: 0.8rem;
  font-weight: 500;
}

.status-badge.active {
  background: #10b981;
  color: white;
}

.status-badge.inactive {
  background: #6b7280;
  color: white;
}

.card-stats {
  display: flex;
  gap: 2rem;
  margin-bottom: 1.5rem;
  padding: 1rem;
  background: #f9fafb;
  border-radius: 8px;
}

.stat {
  text-align: center;
}

.stat-number {
  display: block;
  font-size: 1.5rem;
  font-weight: bold;
  color: var(--primary-color, #3b82f6);
}

.stat-label {
  font-size: 0.8rem;
  color: #6b7280;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.card-actions {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  text-decoration: none;
  font-weight: 500;
  transition: all 0.2s;
  border: none;
  cursor: pointer;
  font-size: 0.875rem;
}

.btn-sm {
  padding: 0.25rem 0.75rem;
  font-size: 0.8rem;
}

.btn-primary {
  background: var(--primary-color, #3b82f6);
  color: white;
}

.btn-primary:hover {
  background: var(--secondary-color, #1d4ed8);
  transform: translateY(-1px);
}

.btn-outline {
  border: 1px solid #d1d5db;
  color: #374151;
  background: white;
}

.btn-outline:hover {
  background: #f9fafb;
  border-color: #9ca3af;
}

.btn-danger {
  background: #ef4444;
  color: white;
}

.btn-danger:hover {
  background: #dc2626;
}

.empty-state {
  grid-column: 1 / -1;
  text-align: center;
  padding: 4rem 2rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.empty-icon {
  font-size: 4rem;
  margin-bottom: 1rem;
}

.empty-state h3 {
  color: #1f2937;
  margin: 0 0 1rem 0;
  font-size: 1.5rem;
}

.empty-state p {
  color: #6b7280;
  margin: 0 0 2rem 0;
  font-size: 1.1rem;
}

@media (max-width: 768px) {
  .equipment-types-page {
    padding: 1rem;
  }
  
  .page-header {
    flex-direction: column;
    align-items: stretch;
  }
  
  .equipment-types-grid {
    grid-template-columns: 1fr;
  }
  
  .card-header {
    flex-direction: column;
    align-items: flex-start;
  }
  
  .card-stats {
    flex-direction: column;
    gap: 1rem;
  }
  
  .card-actions {
    justify-content: center;
  }
}
</style>
